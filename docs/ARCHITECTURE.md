# Architecture (mirrors §24)
Frontend (Next.js) -> FastAPI -> {Project Service, RAG Service (LangChain), Blueprint Service (LangGraph)} -> Postgres+pgvector / S3.
See master spec Doc/ for full diagrams.
